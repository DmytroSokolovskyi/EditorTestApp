import { FC } from 'react';
import './Header.scss';

export const Header: FC = () => {
  return (
    <header className="header">
      <span className="header__text">REACT EDITOR Test</span>
    </header>
  );
};
